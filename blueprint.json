{
  "$schema": "https://playground.wordpress.net/blueprint-schema.json",
  "landingPage": "/",
  "login": true,
  "preferredVersions": {
    "php": "8.2",
    "wp": "6.7"
  },
  "steps": [
    {
      "step": "installTheme",
      "themeZipFile": {
        "resource": "url",
        "url": "{{themeZipUrl}}"
      },
      "options": {
        "activate": true
      }
    },
    {
      "step": "importWxr",
      "file": {
        "resource": "url",
        "url": "{{wxrUrl}}"
      }
    },
    {
      "step": "runPHP",
      "code": "<?php require_once 'wordpress/wp-load.php'; update_option('show_on_front', 'page'); $home = get_page_by_path('home-page'); if ($home) update_option('page_on_front', $home->ID); wp_cache_flush(); echo 'Front page set!'; ?>"
    },
    {
      "step": "runPHP",
      "code": "<?php require_once 'wordpress/wp-load.php'; update_option('blogname', 'Seniorbolaget'); update_option('blogdescription', 'Låt våra rutinerade seniorer göra jobbet!'); update_option('permalink_structure', '/%postname%/'); echo 'Site settings updated!'; ?>"
    }
  ]
}
